CONFIG="$(pwd)/$(dirname "${BASH_SOURCE[0]}")/../config/elasticsearch.yml"
echo "Using $CONFIG"
docker run -v "$CONFIG":/usr/share/elasticsearch/config/elasticsearch.yml -p 9200:9200 -p 9300:9300 -e "discovery.type=single-node" elasticsearch:1.7-alpine
